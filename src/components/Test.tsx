// type Props = {}

import { useState } from "react";

function Test() {
	// const [expand, setExpand] = useState(false);
	// return (
	// 	<div className="bg-green-700">
	// 		<motion.div animate={expand ? "animate" : "initial"} variants={cardVariants}>
	// 			<div
	// 				className=" my-6 w-60 h-64 bg-red-500 inline-block"
	// 				onClick={() => setExpand((prev) => !prev)}
	// 			></div>
	// 		</motion.div>
	// 	</div>
	// );

	return <div className="flex items-center justify-center bg-black cursor-pointer pt-4"></div>;
}

export default Test;
